import React from 'react';
import Typography from '@material-ui/core/Typography';

function Cart() {
    return (
        <Typography component="div" style={{ backgroundColor: 'green', height: '80vh' }} />
    );
}

export default Cart;